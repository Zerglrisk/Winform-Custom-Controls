# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'vs2017-win2016'
variables:
  GitHubPat: '5f3b13e8fe233a5f9d5224fcbbfa8612f78da3b6'
  
steps:
- checkout: self
  clean: true
  
- script: |
   git config --global user.email "wmf1235@naver.com"
   git config --global user.name "Zerglrisk"
  displayName: 'Command Line Script'

- task: tsuyoshiushio.shell-exec.shellexec.shellexec@0
  displayName: 'shellexec '
  inputs:
    code: 'git branch -r | grep -v ''\->'' | while read remote; do git branch --track "${remote#origin/}" "$remote"; done'

- script: 'git pull https://$(UserName):$(Password)@github.com/Zerglrisk/Winform-Custom-Controls.git'
  displayName: Pull

- script: |
   git push https://$(UserName):$(Password)@github.com/Zerglrisk/Winform-Custom-Controls.git -u --all
   
  displayName: Push

